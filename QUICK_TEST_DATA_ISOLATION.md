# 快速测试：用户数据隔离

## 修复内容

✅ **已修复**: merchant.html 中的 merchantId 现在从 localStorage 获取用户名
✅ **已创建**: 为两个用户创建了不同的测试数据

## 测试数据

### MurrayRanelagh 的库存（2个产品）
- iPhone 13 Pro (RAN001) - €1000
- iPad Air (RAN002) - €700

### MurrayDundrum 的库存（2个产品）
- Samsung Galaxy S22 (DUN001) - €900
- MacBook Air M2 (DUN002) - €1200

## 测试步骤

### 1. 测试 MurrayRanelagh

1. **登录**
   - 访问: http://localhost:3000/login.html
   - 用户名: `MurrayRanelagh`
   - 密码: `123456`

2. **访问商户页面**
   - 自动跳转到: http://localhost:3000/merchant.html
   - 或手动访问: http://localhost:3000/merchant.html

3. **检查页面标题**
   - 应该显示: "🏢 Welcome MurrayRanelagh"

4. **检查"我的库存"**
   - 点击"我的库存"标签
   - 应该看到 **2 个产品**:
     - iPhone 13 Pro (RAN001)
     - iPad Air (RAN002)
   - **不应该**看到 Samsung 或 MacBook

5. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 标签
   - 应该看到 API 请求使用 `merchantId=MurrayRanelagh`

### 2. 测试 MurrayDundrum

1. **登出**
   - 点击页面右上角的"登出"按钮

2. **登录**
   - 用户名: `MurrayDundrum`
   - 密码: `123456`

3. **访问商户页面**
   - 自动跳转到: http://localhost:3000/merchant.html

4. **检查页面标题**
   - 应该显示: "🏢 Welcome MurrayDundrum"

5. **检查"我的库存"**
   - 点击"我的库存"标签
   - 应该看到 **2 个产品**:
     - Samsung Galaxy S22 (DUN001)
     - MacBook Air M2 (DUN002)
   - **不应该**看到 iPhone 或 iPad

6. **检查浏览器控制台**
   - 应该看到 API 请求使用 `merchantId=MurrayDundrum`

### 3. 检查服务器日志

打开服务器终端，应该看到类似的日志：

```
✅ 用户 MurrayRanelagh 只能查看自己的数据
✅ 用户 MurrayDundrum 只能查看自己的数据
```

而不是：
```
⚠️  用户 merchant_001 不在 UserNew 表中，使用默认隔离策略
```

## 预期结果

| 用户 | 应该看到的产品 | 不应该看到的产品 |
|------|----------------|------------------|
| MurrayRanelagh | iPhone 13 Pro, iPad Air | Samsung Galaxy S22, MacBook Air M2 |
| MurrayDundrum | Samsung Galaxy S22, MacBook Air M2 | iPhone 13 Pro, iPad Air |

## 验证清单

- [ ] MurrayRanelagh 登录后页面标题显示正确
- [ ] MurrayRanelagh 只能看到自己的 2 个产品
- [ ] MurrayRanelagh 不能看到 MurrayDundrum 的产品
- [ ] MurrayDundrum 登录后页面标题显示正确
- [ ] MurrayDundrum 只能看到自己的 2 个产品
- [ ] MurrayDundrum 不能看到 MurrayRanelagh 的产品
- [ ] 服务器日志显示正确的用户名
- [ ] 浏览器控制台显示正确的 merchantId

## 故障排除

### 问题：仍然看到相同的数据

**解决方案**:
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 硬刷新页面（Ctrl+F5）
3. 检查 localStorage: 在控制台输入 `localStorage.getItem('username')`
4. 确认返回正确的用户名

### 问题：看不到任何数据

**解决方案**:
1. 检查是否已运行 `node create-test-inventory.js`
2. 检查数据库连接是否正常
3. 运行 `node check-user-data.js` 验证数据

### 问题：页面标题仍显示 "批发商户中心"

**解决方案**:
1. 确认已修改 merchant.html
2. 硬刷新页面（Ctrl+F5）
3. 检查 `loadMerchantInfo()` 函数是否被调用

## 下一步测试

### 测试组内共享（可选）

如果想测试组内共享功能：

1. 创建一个店铺组
2. 将两个用户分配到同一个组
3. 确保 `canViewGroupInventory = true`
4. 重新登录测试
5. 两个用户应该都能看到 4 个产品（2+2）

## 重置测试数据

如果需要重置测试数据：

```javascript
// 删除测试数据
const MerchantInventory = require('./models/MerchantInventory');
await MerchantInventory.deleteMany({ 
  merchantId: { $in: ['MurrayRanelagh', 'MurrayDundrum'] } 
});

// 重新运行创建脚本
node create-test-inventory.js
```

## 成功标志

✅ 两个用户看到**不同**的库存数据
✅ 每个用户只能看到自己的数据
✅ 数据隔离功能正常工作
